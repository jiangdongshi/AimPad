-- ============================================
-- AimPad 管理员角色增量脚本
-- 需要在 aimpad 数据库中执行
-- ============================================

USE `aimpad`;

-- --------------------------------------------
-- 1. 为 users 表添加 role 字段
-- --------------------------------------------
-- 注意：如果执行时报错 "Duplicate column name"，说明字段已存在，忽略该错误即可
ALTER TABLE `users`
  ADD COLUMN `role` VARCHAR(16) NOT NULL DEFAULT 'user' COMMENT '角色 (user/admin)'
  AFTER `last_login_at`;

-- --------------------------------------------
-- 2. 创建默认管理员账号（可选）
--    将下面的邮箱替换为实际管理员邮箱
-- --------------------------------------------
-- UPDATE `users` SET `role` = 'admin' WHERE `email` = 'admin@example.com';

-- --------------------------------------------
-- 3. 验证
-- --------------------------------------------
SELECT 'users 表结构 (role 字段):' AS '';
DESCRIBE `users`;

-- ============================================
-- 执行完毕
-- 如需撤销此脚本，执行:
-- ALTER TABLE `users` DROP COLUMN `role`;
-- ============================================
